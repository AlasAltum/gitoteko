# # # Core CLI defaults (used when CLI args are omitted)
# GIT_PROVIDER=bitbucket
# GIT_WORKSPACE=your-workspace
# BASE_DIR=/tmp/gitoteko-workspace

# # # Optional: limit execution to a single repository slug
# GIT_REPO_SLUG=repo-slug

# # # Optional: limit to N repositories
# GIT_MAX_REPOS=3
# # # Selection mode when limiting: first|random
# GIT_REPO_SELECTION=first
# # # Optional deterministic seed for random mode
# GIT_RANDOM_SEED=42
# # # Optional: stop at first repository failure (default false)
# GIT_STOP_ON_ERROR=false

# # # Optional: action pipeline composition (comma-separated)
# # # Available actions:
# # # - detect-languages
# # # - write-language-csv
# # # - generate-sonar-properties
# # # - run-sonar-scan
# GIT_ACTIONS=detect-languages,generate-sonar-properties,run-sonar-scan

# # # Bitbucket provider authentication (choose one approach)
# # # Preferred: token
BITBUCKET_TOKEN=replace-with-your-bitbucket-token

# # # Alternative: app password auth
# BITBUCKET_USERNAME=your-username
# BITBUCKET_APP_PASSWORD=your-app-password

# # # Optional Bitbucket provider runtime settings
# BITBUCKET_API_BASE_URL=https://api.bitbucket.org/2.0
# BITBUCKET_TIMEOU_SECONDS=30

# # # Rule-specific examples (not part of core CLI contract)
# LANGUAGE_DETECTION_EXTENSIONS=.ts,.js,.java,.py
# LANGUAGE_REPORT_CSV=/tmp/gitoteko-workspace/languages.csv
# LANGUAGE_REPORT_REGENERATE=false
# SONAR_PROPERTIES_OVERWRITE=false
# SONAR_JAVA_BINARIES_PATH=target/classes
# SONARQUBE_URL=https://sonarqube.example.com
# SONARQUBE_TOKEN=replace-with-your-sonar-token
# SONAR_SCANNER_EXECUTABLE=sonar-scanner
# SONAR_SCANNER_TIMEOUT_SECONDS=1800
# SONAR_WAIT_MODE=sync
# SONAR_SUBMISSION_DELAY_SECONDS=10
# SONAR_SYNC_POLL_INTERVAL_SECONDS=5
# SONAR_SYNC_TIMEOUT_SECONDS=1800
# SONAR_SKIP_UNCHANGED=true
# SONAR_FORCE_SCAN=false
# SONAR_STATE_FILE=.git/gitoteko_sonar_state.json
